/* global angular */
(function () {
    "use strict";

    angular.module('angular-seed.module')
        .controller('ModuleCtrl', function ($log) {

            var LOG = $log.getInstance('ModuleCtrl');

            var ctrl = this;

        });

}());